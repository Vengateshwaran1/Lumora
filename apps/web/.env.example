# For running the Vite dev server directly on the host, outside Docker
# Compose. Copy to apps/web/.env.local. Vite only exposes variables
# prefixed with VITE_ to client code.

VITE_API_BASE_URL=http://localhost:8000/api/v1
