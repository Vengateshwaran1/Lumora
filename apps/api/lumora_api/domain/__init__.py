"""Domain layer: entities, value objects, and domain services.

Pure Python — no FastAPI, SQLAlchemy, or other framework imports. This is
what keeps business rules unit-testable without a database or HTTP server.
Populated starting M1 as features land; empty in Milestone 0.
"""
