"""Application layer: use cases that orchestrate domain logic and ports.

Depends on `domain`; depended on by `api`. Implementations of the ports
defined here live in `infrastructure`. Empty in Milestone 0.
"""
